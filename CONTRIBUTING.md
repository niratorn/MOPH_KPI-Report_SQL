# แนวทางการมีส่วนร่วม (Contributing)

ขอบคุณที่สนใจแบ่งปัน query ตัวชี้วัดให้เพื่อนเจ้าหน้าที่ IT โรงพยาบาลทั่วประเทศ
เอกสารนี้อธิบายวิธีส่งงานเข้าชุมชนทีละขั้น ทั้งแบบไม่ต้องใช้ git และแบบใช้ git

ก่อนเริ่ม กรุณาอ่าน 2 เอกสารนี้ก่อน:

- [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) : กติกาการอยู่ร่วมกันของชุมชน
- [docs/data-privacy.md](docs/data-privacy.md) : ข้อกำหนดเรื่องข้อมูลส่วนบุคคล ห้ามมีข้อมูลผู้ป่วยจริงในทุกไฟล์และทุก issue

ข้อควรจำ: query ในชุมชนนี้ใช้เพื่อตรวจสอบและเตรียมข้อมูลภายในโรงพยาบาล ไม่ใช่ค่าทางการ ค่าทางการอ้างอิงจากระบบ HDC ของกระทรวง (https://hdcservice.moph.go.th)

## แบ่งปันได้ 3 ช่องทาง

1. **ขอ query ที่ยังไม่มี** : เปิด issue ด้วยฟอร์ม "ขอ Query ตัวชี้วัด" บอกชื่อตัวชี้วัด หมวด และแหล่งนิยาม แล้วรอสมาชิกชุมชนช่วยเขียน
2. **ส่ง query ของท่านโดยไม่ต้องใช้ git** : เปิด issue ด้วยฟอร์ม "แบ่งปัน Query (ไม่ต้องใช้ git)" วาง SQL ลงในฟอร์มได้เลย maintainer จะจัดรูปแบบและนำเข้า repository ให้ โดยให้เครดิตท่านในไฟล์
3. **ส่ง Pull Request** : สำหรับผู้ที่ใช้ git เป็น หรืออยากลองทำตามขั้นตอนหน้าเว็บด้านล่าง วิธีนี้ชื่อของท่านจะปรากฏใน history ของ repository โดยตรง

ไม่ว่าช่องทางไหน เนื้อหาที่ส่งต้องผ่าน checklist ในหัวข้อ "สิ่งที่ query ต้องมีก่อนส่ง" ด้านล่าง

## ส่งผ่านหน้าเว็บ GitHub แบบละเอียด (ไม่ต้องใช้ git)

วิธีนี้ใช้แค่เว็บเบราว์เซอร์ ไม่ต้องติดตั้งโปรแกรมใด ๆ ทำตามทีละขั้น:

### ขั้นที่ 1 : สมัครหรือล็อกอิน GitHub

1. เปิดเว็บไซต์ GitHub ในเบราว์เซอร์
2. ถ้ายังไม่มีบัญชี กดปุ่ม **Sign up** มุมขวาบน สมัครด้วยอีเมล (ฟรี) แล้วยืนยันอีเมลตามที่ระบบแจ้ง
3. ถ้ามีบัญชีแล้ว กด **Sign in** แล้วล็อกอินตามปกติ

### ขั้นที่ 2 : Fork repository

1. เปิดหน้า repository นี้ (MOPH_KPI-Report_SQL)
2. กดปุ่ม **Fork** ที่มุมขวาบนของหน้า
3. ในหน้าถัดไป ไม่ต้องแก้อะไร กดปุ่ม **Create fork**
4. ระบบจะพาท่านไปยังสำเนาของ repository ในบัญชีของท่านเอง สังเกตว่า URL จะขึ้นต้นด้วยชื่อบัญชีของท่าน ไม่ใช่ชื่อเจ้าของเดิม สำเนานี้เรียกว่า "fork" ท่านแก้ไขอะไรใน fork ก็ได้โดยไม่กระทบ repository หลัก

### ขั้นที่ 3 : สร้างไฟล์ query ใน fork ของท่าน

1. ใน fork ของท่าน กดปุ่ม **Add file** (อยู่ใกล้ปุ่มสีเขียว Code) แล้วเลือก **Create new file**
2. ในช่องชื่อไฟล์ด้านบน พิมพ์ path เต็มของไฟล์ เช่น

   ```
   queries/mch-anc/anc-coverage/hosxp.sql
   ```

   เคล็ดลับ: ทุกครั้งที่พิมพ์เครื่องหมาย `/` GitHub จะสร้างโฟลเดอร์ใหม่ให้อัตโนมัติ ท่านจึงสร้างโฟลเดอร์ซ้อนกันกี่ชั้นก็ได้จากช่องนี้ช่องเดียว
3. คัดลอกเนื้อหาจาก [templates/query-template.sql](templates/query-template.sql) มาวางในช่องแก้ไขด้านล่าง แล้วแก้เป็น query ของท่าน กรอก header metadata ให้ครบทุกบรรทัด
4. กดปุ่มสีเขียว **Commit changes...** มุมขวาบน
5. ในหน้าต่างที่เด้งขึ้นมา ใส่ข้อความสั้น ๆ ในช่อง commit message เช่น "เพิ่ม query ANC coverage" แล้วกดปุ่ม **Commit changes** อีกครั้ง

### ขั้นที่ 4 : สร้าง README.md คู่กัน

ทำซ้ำขั้นที่ 3 อีกรอบ แต่พิมพ์ path เป็น README.md ในโฟลเดอร์เดียวกัน เช่น

```
queries/mch-anc/anc-coverage/README.md
```

เนื้อหา README ต้องอธิบายนิยามตัวชี้วัด พารามิเตอร์ที่ต้องปรับ และข้อจำกัด (ดูตัวอย่างจาก [queries/mch-anc/maternal-anemia/README.md](queries/mch-anc/maternal-anemia/README.md))

### ขั้นที่ 5 : เปิด Pull Request

1. กลับไปหน้าแรกของ fork ของท่าน (กดชื่อ repository ด้านบนซ้าย)
2. กดปุ่ม **Contribute** (อยู่เหนือรายการไฟล์) แล้วเลือก **Open pull request**
3. ระบบจะแสดงหน้าสรุปสิ่งที่ท่านแก้ไข ตรวจดูว่ามีเฉพาะไฟล์ที่ตั้งใจส่ง
4. ใส่ชื่อ PR ให้สื่อความ เช่น "เพิ่ม query: ANC coverage" แล้วกรอกข้อมูลตาม template ที่ขึ้นมาอัตโนมัติ ติ๊ก checklist ให้ครบ
5. กดปุ่ม **Create pull request**

### ขั้นที่ 6 : รอ review และตอบคำถาม

maintainer หรือสมาชิกชุมชนอาจ comment ถามหรือขอให้แก้ไข ท่านตอบได้ในหน้า PR โดยตรง
ถ้าต้องแก้ไฟล์ ให้กลับไปที่ fork ของท่าน เปิดไฟล์นั้น กดไอคอนรูปดินสอ (Edit this file) แก้แล้ว commit อีกครั้ง การแก้ไขจะเข้าไปอยู่ใน PR เดิมโดยอัตโนมัติ ไม่ต้องเปิด PR ใหม่

หากขั้นตอนเหล่านี้ยังยุ่งยากเกินไป ใช้ช่องทางที่ 2 (issue ฟอร์ม "แบ่งปัน Query") ได้เสมอ ไม่ต้องเกรงใจ

## ส่งผ่าน git

สำหรับผู้ที่ใช้ git เป็นอยู่แล้ว ใช้ขั้นตอน fork, branch, PR ตามมาตรฐาน:

```bash
# 1. Fork repository นี้ผ่านหน้าเว็บ GitHub ก่อน แล้ว clone fork ของท่าน
git clone <URL ของ fork ของท่าน>
cd MOPH_KPI-Report_SQL

# 2. สร้าง branch ใหม่สำหรับงานนี้
git checkout -b add-anc-coverage

# 3. เพิ่มไฟล์ hosxp.sql และ README.md ในโฟลเดอร์ของตัวชี้วัด
#    เริ่มจาก templates/query-template.sql
git add queries/mch-anc/anc-coverage/

# 4. Commit และ push
git commit -m "เพิ่ม query: ANC coverage"
git push origin add-anc-coverage

# 5. เปิด Pull Request จาก branch นี้ไปยัง branch หลักของ repository ต้นทาง
#    ผ่านหน้าเว็บ GitHub
```

## สิ่งที่ query ต้องมีก่อนส่ง (checklist)

ก่อนส่งทุกครั้ง ตรวจตามรายการนี้:

- [ ] header metadata ครบทุกบรรทัดตาม template ใน [templates/query-template.sql](templates/query-template.sql)
- [ ] ชื่อโฟลเดอร์เป็น kebab-case ภาษาอังกฤษ เช่น `maternal-anemia`, `preterm-birth` (ตัวพิมพ์เล็ก คั่นคำด้วยขีดกลาง)
- [ ] มี README.md ในโฟลเดอร์เดียวกัน อธิบายนิยามตัวชี้วัด พารามิเตอร์ที่ต้องตั้งค่า และข้อจำกัดของ query
- [ ] ไม่มีข้อมูลผู้ป่วยจริงในทุกไฟล์ ตัวอย่างผลลัพธ์ต้องเป็นข้อมูลสมมติเท่านั้น (HN แบบ 000001, 000002) และติดป้ายกำกับว่า "ข้อมูลสมมติ" ดูรายละเอียดใน [docs/data-privacy.md](docs/data-privacy.md)
- [ ] ระบุรุ่น HOSxP ที่ทดสอบในบรรทัด "ทดสอบกับรุ่น" ของ header (ถ้ายังไม่ได้ทดสอบกับฐานข้อมูลจริง ให้เขียนตามจริง)
- [ ] ทุกจุดที่ต้องปรับตามโรงพยาบาล เช่น รหัส lab, ชื่อ ward, รหัสแผนก มี comment เครื่องหมาย `[ปรับตามโรงพยาบาล]` กำกับพร้อมคำอธิบาย
- [ ] ระบุแหล่งอ้างอิงนิยามที่ตรวจสอบได้ เช่น template ตัวชี้วัดของปีงบประมาณปัจจุบันใน HDC หรือ Health KPI หรือแหล่งวิชาการจริง ห้ามอ้างอิงลอย ๆ หรือแต่งแหล่งอ้างอิงขึ้นเอง
- [ ] README ระบุชัดว่า query นี้ใช้เพื่อตรวจสอบและเตรียมข้อมูลภายในโรงพยาบาล ไม่ใช่ค่าทางการ ค่าทางการอ้างอิงจากระบบ HDC
- [ ] SQL ทำงานได้บน MySQL 5.x (ไม่ใช้ CTE และ window function) ตาม [docs/query-style-guide.md](docs/query-style-guide.md)

รายละเอียดรูปแบบการเขียน SQL ดูที่ [docs/query-style-guide.md](docs/query-style-guide.md)

## การ review

ขั้นตอนหลังจากท่านส่งงาน:

1. **ตรวจรูปแบบ** : maintainer ตรวจตาม checklist ด้านบน เช่น header metadata, การตั้งชื่อโฟลเดอร์, การไม่มีข้อมูลผู้ป่วยจริง อาจมี comment ขอให้แก้ไข
2. **Peer validation** : maintainer จะขออาสาสมัครจากโรงพยาบาลอื่นช่วยรัน query กับฐานข้อมูลของตน แล้วรายงานผลกลับ ผลการทดสอบจะถูกบันทึกในบรรทัด "ทดสอบกับรุ่น" ของไฟล์ .sql
3. **สถานะของ query** : query ใหม่ทุกตัวเริ่มที่สถานะ `draft` และจะเปลี่ยนเป็น `validated` เมื่อมีโรงพยาบาลอื่นอย่างน้อย 1 แห่ง (นอกเหนือจากผู้เขียน) รันแล้วยืนยันว่าผลลัพธ์สอดคล้องกับข้อมูลจริงของตน

การ review ทั้งหมดทำอย่างเปิดเผยในหน้า PR หรือ issue เพื่อให้ชุมชนเรียนรู้ร่วมกัน กรุณาสื่อสารกันตามกติกาใน [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)
