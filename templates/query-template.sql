-- ============================================================
-- ชื่อตัวชี้วัด :
-- หมวด        :
-- HIS         : HOSxP (MySQL/MariaDB)
-- ทดสอบกับรุ่น  : ยังไม่มีการยืนยันจากโรงพยาบาลจริง (รอ community ช่วยทดสอบ)
-- สถานะ       : draft
-- ผู้เขียน      : MOPH KPI SQL Community
-- อัปเดตล่าสุด  : 2026-07-18
-- นิยามอ้างอิง  : (สรุปสั้น + ให้ดู README ในโฟลเดอร์เดียวกัน)
-- สิ่งที่ต้องปรับตามโรงพยาบาล : (list)
-- ข้อจำกัด     : (list สั้น)
-- ============================================================

-- ============================================================
-- พารามิเตอร์ (แก้ค่าที่นี่ที่เดียว ไม่ต้องไล่แก้ใน query)
-- ============================================================

SET @start_date := '2025-10-01';  -- ปีงบประมาณ 2569 : เริ่ม 1 ต.ค. 2568
SET @end_date   := '2026-09-30';  -- ปีงบประมาณ 2569 : สิ้นสุด 30 ก.ย. 2569

SET @lab_code_example := '0000';  -- [ปรับตามโรงพยาบาล] รหัส lab_items_code ของรายการ lab ที่ใช้
                                  -- หาได้จาก: SELECT lab_items_code, lab_items_name
                                  --           FROM lab_items
                                  --           WHERE lab_items_name LIKE '%คำค้น%';

-- ============================================================
-- 1) Query สรุป (ตัวตั้ง/ตัวหาร/ร้อยละ)
-- ============================================================
-- TODO: เขียน query หลักที่จบด้วยตารางสรุป 3 คอลัมน์:
--       ตัวตั้ง (numerator), ตัวหาร (denominator), ร้อยละ
-- TODO: ห้ามใช้ CTE (WITH) และ window functions เพื่อรองรับ MySQL 5.x
--       ให้ใช้ derived table (subquery ใน FROM) แทน
-- TODO: ใส่ comment -- [ปรับตามโรงพยาบาล] ทุกจุดที่แต่ละโรงพยาบาลต้องแก้
-- TODO: คำนวณร้อยละต้องหารด้วย NULLIF(ตัวหาร, 0) เสมอ กัน divide-by-zero
-- TODO: ก่อน CAST ค่า lab_order_result ต้องกรองด้วย REGEXP '^[0-9]+([.][0-9]+)?$'
--       (ใช้ [.] ห้ามใช้ \. เพราะ MySQL ตัด backslash ใน string ทำให้จุด match ทุกตัวอักษร)
-- TODO: เทียบรหัส icd10 ให้ใช้ REPLACE(icd10, '.', '') เสมอ
--       เพื่อรองรับทั้งโรงพยาบาลที่เก็บรหัสแบบมีจุดและไม่มีจุด
-- หมายเหตุ: ผลจาก query นี้ใช้ตรวจสอบภายในเท่านั้น ค่าทางการอ้างอิงจากระบบ HDC

-- SELECT
--     ...  AS ตัวตั้ง,
--     ...  AS ตัวหาร,
--     ...  AS ร้อยละ
-- FROM ( ... ) t;

-- ============================================================
-- 2) Query รายละเอียดรายบุคคล (ใช้ภายใน ห้ามแชร์)
-- ============================================================
-- คำเตือน: ผลรายบุคคลมีข้อมูลส่วนบุคคลของผู้ป่วย
--          ใช้ตรวจสอบภายในโรงพยาบาลเท่านั้น ห้ามแชร์ออกนอกโรงพยาบาล
--          (ดู docs/data-privacy.md)
-- TODO: เขียน query รายบุคคลให้เงื่อนไขตรงกับ query สรุปทุกประการ
--       เพื่อใช้ไล่ตรวจว่าใครถูกนับในตัวตั้ง/ตัวหาร
-- TODO: ระหว่างพัฒนา ให้เติม LIMIT 100 ท้าย query เพื่อลดภาระฐานข้อมูล

-- SELECT
--     p.hn,
--     ...
-- FROM patient p
--     ...
-- WHERE ...
-- ORDER BY ...;
